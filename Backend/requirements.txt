# AuraNet — AI-assisted Planetary Image Enhancement Pipeline
# Core numerical / deep learning stack
torch>=2.1.0
torchvision>=0.16.0

# Geospatial raster I/O (reads/writes GeoTIFF, preserves CRS/transform/metadata)
rasterio>=1.3.9

# Array / scientific computing
numpy>=1.24.0
scipy>=1.11.0

# Classical image processing + reference IQA metrics (PSNR, SSIM)
scikit-image>=0.22.0

# Generic TIFF / PDS4-adjacent raster reading fallback (when rasterio/GDAL
# can't parse a vendor-specific 16-bit product)
tifffile>=2023.9.26

# Stationary Wavelet Transform (SWT) / Inverse SWT for band decomposition
PyWavelets>=1.4.1

# No-reference IQA (BRISQUE) with GPU-friendly torch backend
piq>=0.8.0

# Config parsing
PyYAML>=6.0

# Progress bars for batch runs
tqdm>=4.66.0
